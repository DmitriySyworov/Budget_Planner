{{- range .Values.statefulServices }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}-service
spec:
  clusterIP: None
  selector:
    app: {{ .name }}
  ports:
    - protocol: TCP
      port: {{ .port }}
      targetPort: {{ .port }}
  type: ClusterIP
---
{{- end }}