apiVersion: v1
kind: Service
metadata:
  name: service-budget-planner
spec:
  selector:
    app: app-budget-planner
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32000
  type : NodePort